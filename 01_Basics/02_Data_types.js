"use Strinct"; // treat all js code as newer version 
// alert(3+3) // we are using note js, not browser

let age=18
let isLoggined=false
let state = null

let x; //undefined

console.log(typeof undefined ) //Undefine
console.log(typeof null) //object
